app.views.User = {
    login : function (params) {
        /** @params
          * @params.confirm : callback a ser chamado ao submit, recebe os dados do post
          */
    },
    header : function (params) {
        /** @params
          * @params.data.user : usuário
          * @params.logout : callback a ser chamado quando clicar em logout
          */
    }
}